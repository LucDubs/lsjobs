<%= form_with(model: aplicant) do |form| %>
  <% if aplicant.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(aplicant.errors.count, "error") %> prohibited this aplicant from being saved:</h2>

      <ul>
        <% aplicant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :vacancy_id, style: "display: block" %>
    <%= form.text_field :vacancy_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
